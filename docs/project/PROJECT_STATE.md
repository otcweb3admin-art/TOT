# PROJECT_STATE.md — 项目当前状态（交接棒）

> **活文档**：每个工作块结束更新。接手本项目请先读 [/CLAUDE.md](../../CLAUDE.md)，再读本文件。
> 本文件只回答："**现在进展到哪 · 下一步做什么 · 有哪些待决问题**"。规则与文档全貌见 CLAUDE.md。
> 最后更新：2026-06-04　｜　对应提交：见 `git log -1`（本文件随附提交）

---

## 一句话状态
业务架构已成体系（约 22 份）。**P0 地基完成并上线**（`tot-dun.vercel.app/health` → `db:connected`）。**P1 身份与角色地基已实现，且本地登录端到端验证通过**（2026-06-03 浏览器实跑 `login→dashboard→logout→守卫` 全绿；anon key 用新版 `sb_publishable_` 格式，`supabase-js`/`@supabase/ssr` 均兼容）。**线上登录亦端到端验证通过**（2026-06-03，TASK-026：线上 `/dashboard` 带 session 返回 200 并显示 email/role/status）。**P2-001 Merchant Intake Foundation 完成**（TASK-027）+ **P2-002 Merchant Profile Asset Foundation 完成**（2026-06-03，TASK-028：第一类商家资产 `MerchantProfile`，1-1 摘要级画像，创建/查看/更新闭环，本地手工 10 项全绿）。**P2-003 Merchant Baseline Metric Foundation 完成**（2026-06-03，TASK-029：第一块增长验证基线 `MerchantBaselineMetric`，含数字字段校验，本地手工 11 项全绿）。**P2-004 TB-001 Minimal Intake Foundation 完成**（2026-06-03，TASK-030：TB-001 最小人工诊断 `MerchantDiagnosis`，1-1、可引用当前画像+基准作上游输入、创建/更新闭环）。**P2-005 TB-002 Account Setup Foundation 完成**（2026-06-03，TASK-031：第二个模板节点资产 `MerchantAccountSetup`，1-1、可引用当前 TB-001 诊断、创建/更新闭环，本地手工 12 项全绿）。**P2-006 TB-003 Material Collection Foundation 完成**（2026-06-03，TASK-032：第三个模板节点资产 `MerchantMaterialCollection`，1-1、可引用当前 TB-002 账号搭建、创建/更新闭环，本地手工 12 项全绿）。**P2-007 TB-004 Content Operation Foundation 完成**（2026-06-03，TASK-033：第四个模板节点资产 `MerchantContentOperation`，1-1、可引用当前 TB-003 素材采集、创建/更新闭环，本地手工 12 项全绿）。**P2-008 TB-005 Live Planning Foundation 完成**（2026-06-03，TASK-034：第五个模板节点资产 `MerchantLivePlanning`，1-1、可引用当前 TB-004 内容运营、创建/更新闭环，本地手工 12 项全绿）。**P2-009 TB-006 Lead Conversion Foundation 完成**（2026-06-03，TASK-035：第六个模板节点资产 `MerchantLeadConversion`，1-1、**双上游引用**当前 TB-004 内容运营 + TB-005 直播规划、创建/更新闭环，本地手工 14 项全绿）。**P2-010 TB-007 Data Review Foundation 完成**（2026-06-03，TASK-036：第七个模板节点资产 `MerchantDataReview`，1-1、**四上游引用**当前基准 + TB-004/005/006、创建/更新闭环，本地手工 18 项全绿）。**P2-011 TB-008 90-Day Growth Plan Foundation 完成**（2026-06-03，TASK-037：第八个模板节点资产 `MerchantNinetyDayGrowthPlan`，1-1、**四上游引用**当前基准 + TB-001/006/007、创建/更新闭环，本地手工 18 项全绿）。**🎯 至此 TB-001~008 模板节点最小地基全链完成**。**P2-012 最小商家级权限隔离完成**（2026-06-04，TASK-040：`admin` 全见 / 其它角色仅自有 `owner|createdBy`；统一 helper；未改 schema；回归 Tier1 谓词矩阵 + Tier2 真实 HTTP 全绿）。**P2-013 商家工作台/节点总览完成**（2026-06-04，TASK-041：每商家一个 `/workspace` 总览页——基础信息 + Profile→Baseline→TB-001~008 全链状态/快速入口/首个缺失节点/规则型下一步提示；只读 helper，复用权限过滤；未改 schema/业务模型）。下一步：P2-014（角色细分 · 某 TB 深化 · MVS · action-helper 二轮重构 等，待用户定）。

## 当前阶段
- **文档/架构**：Phase 0–2 + 增长运行机制六层闭环，已相当完整。
- **代码**：**P0 完成（含线上验证）；P1 Auth+角色地基代码完成**（migration 已应用、build/lint/守卫验证通过），**运行时登录待 Supabase Auth 配置**。

## 已完成（指针见 CLAUDE.md「文档地图」）
- 模板 TB-001~008 + 治理 + 全套业务架构（地基/集成/交付运营/增长运行机制六层）。
- **P0 地基（代码·TASK-022/024）**：Next 16 + Prisma ^6 + `HealthCheck` + `/health`；连 Supabase 跑通迁移；**Vercel 上线**（`tot-dun.vercel.app`，Root Directory=`app` 是关键）。
- **P1 Auth+角色地基（代码·TASK-025）**：
  - 依赖：`@supabase/ssr` + `@supabase/supabase-js`。
  - Supabase 工具：`lib/supabase/{config,server,client}.ts`（含 `isSupabaseConfigured()` 优雅降级）。
  - 身份层：`lib/auth/dal.ts`（`getCurrentUser`/`requireUser`，首登 JIT 建 `UserProfile`）、`lib/auth/actions.ts`（`login`/`logout` Server Actions）。
  - 路由守卫：**`app/proxy.ts`**（Next 16 把 middleware 改名 proxy；保护 `/dashboard`、已登录访问 `/login` 跳 `/dashboard`、`/health` 不保护）。
  - 页面：`/login`（邮箱密码 + 错误显示）、`/dashboard`（受保护空壳，显示 email/role/status/阶段 + 登出）。
  - 数据：migration `20260602230255_p1_auth_role_foundation` 建 `UserProfile` + `Role`(6)/`UserStatus`(2) 枚举。
  - 验证：build ✓ / lint ✓ / 守卫 ✓（未登录 `/dashboard`→307→`/login`；`/login`200；`/health`200）。
- **基础设施**：自动推送 hook、内置记忆、CLAUDE.md 接续入口、本交接棒。

## 进行中 / 下一步
- **P1 收尾 — 本地登录端到端已验证 ✅**（2026-06-03，浏览器实跑）：
  1. ✅ 测试用户 `admin@tot.local`/`TotAdmin#2026`（DB 直建：已确认 + bcrypt + email identity；测试用，随时改/删）。
  2. ✅ `app/.env` 已填 `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY`（新版 `sb_publishable_`）+ `SUPABASE_SERVICE_ROLE_KEY`（新版 `sb_secret_`，P1 未用）。
  3. ✅ 实跑链路：`/login` 填表单 → `POST /login 303 ƒ login()` → `/dashboard`（显示 `admin@tot.local / operator / active`）→ 登出 `POST /dashboard 303 ƒ logout()` → `/login` → 登出后访问 `/dashboard` 被守卫弹回 `/login`。DAL 首登建档 `UserProfile{operator,active}` 生效。
  - ⚠️ **DB 直建用户的坑**：GoTrue 把 `confirmation_token/recovery_token/email_change/...` 等列按"非空字符串"读取，手工插入留 `NULL` 会致 `500 Database error querying schema`；已 `COALESCE` 补空串、`email_change_confirm_status` 补 0 修复（详见 CLAUDE.md 决策）。
- **TASK-025 收尾完成 ✅**（2026-06-03）：`npm run build` ✓ / `npm run lint` ✓ / 密钥审计干净（`app/.env` 未入库，仅 `.env.example` 占位；`settings.local.json`【含 Vercel token】已被 gitignore）。P1 代码 commit **`4cf8e88`**（`feat: implement p1 auth role foundation`，已推送）；里程碑 tag **`checkpoint-p1-auth-final`**（回滚还原点）。
- **P1 Online Auth Verification completed ✅**（2026-06-03，TASK-026）：用户已配 Vercel `NEXT_PUBLIC_SUPABASE_URL`+`NEXT_PUBLIC_SUPABASE_ANON_KEY` 且已生效。验证方式（无可用浏览器 → 用 `@supabase/ssr` 同款库生成真实会话 cookie + curl 打线上）：`/health`=connected、`/login`=200、**带 session 的线上 `/dashboard`=HTTP 200** 且渲染 `admin@tot.local / operator / active / P1 Auth Foundation`、未登录 `/dashboard`→307 `/login`；logout 本地实跑通过（线上同代码）。tag `checkpoint-p1-online-verified`。
- **P2-001 Merchant Intake Foundation 完成 ✅**（2026-06-03，TASK-027）：第一个业务**根实体 `Merchant`**（migration `20260603120318_p2_merchant_intake_foundation`；`MerchantStatus{lead,active,paused,archived}`；owner/createdBy → `UserProfile.id`；`@@index` on owner/createdBy/status）。
  - 页面：`/dashboard/merchants`（列表：名称/行业/城市·国家/状态/创建时间）、`/dashboard/merchants/new`（创建表单 + Server Action `createMerchant`，内置 `requireUser()` 守卫、owner=createdBy=当前 profile、status 默认 `lead`）、`/dashboard/merchants/[id]`（详情 + TB-001/诊断/策略/计划/执行/监控/复盘 占位「未开始」）；dashboard 加商家入口 + P2 阶段提示。
  - DAL 增 `profileId`（= `UserProfile.id`，区别于 Supabase auth id）。
  - **边界守住**：未放 TB-001 / Profile Asset 字段（客群画像/卖点/增长目标/投流预算/诊断评分）；**列表暂不做权限过滤**（`data.ts` 注释 TODO，待权限模型）。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（3 migrations, up to date）✓ / 本地手工：登录→列表→建商家→详情→DB 记录(count=1)→未登录 307。tag `checkpoint-p2-001-merchant-intake`。
  - 测试数据：Supabase 现有 1 条测试商家「测试商家A (Toronto Diner)」（验证夹具，可删）。
- **P2-002 Merchant Profile Asset Foundation 完成 ✅**（2026-06-03，TASK-028）：第一类**商家资产 `MerchantProfile`**（migration `20260603123223_p2_merchant_profile_asset_foundation`；**1-1** with Merchant，`@unique merchantId` + `onDelete: Cascade`；9 个**摘要级**字段；createdBy/updatedBy → `UserProfile.id`）。
  - 页面：`/dashboard/merchants/[id]/profile`（创建/编辑表单，`saveMerchantProfile` upsert Server Action，merchantId 服务端 bind、内置 `requireUser()` 守卫）；商家详情页加「商家画像」区块（有则显示摘要 + 更新人/时间 + 编辑入口；无则「暂无画像 + 创建」）。
  - DAL `getMerchantById` 增 include `profile`（含 updatedBy）。
  - **边界守住**：仅**摘要级**字段，**非完整 TB-001**（无完整客群/卖点矩阵、P-001 预算、诊断评分、AI 输出）；权限仍简化（登录即可编辑任意商家画像，`profile-actions.ts` 注释 TODO 待权限模型）。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（4 migrations, up to date）✓ / 本地手工 10 项全绿（登录→详情→创建画像→填写→保存回详情→详情显示摘要→再次编辑 growthGoal→+40%→DB `MerchantProfile`(count=1, merchantId 匹配, updated_after_create)→createdBy=updatedBy=当前 profile→未登录 307）。tag `checkpoint-p2-002-merchant-profile`。
- **P2-003 Merchant Baseline Metric Foundation 完成 ✅**（2026-06-03，TASK-029）：第一块**增长验证基线 `MerchantBaselineMetric`**（migration `20260603125448_p2_merchant_baseline_metric_foundation`；**1-1** with Merchant，`@unique`+`onDelete:Cascade`；Decimal/Int 数值字段 + `BaselineDataConfidence{unknown,low,medium,high}` 枚举；createdBy/updatedBy → `UserProfile.id`）。
  - 页面：`/dashboard/merchants/[id]/baseline`（创建/编辑表单，`saveMerchantBaselineMetric` upsert，merchantId 服务端 bind、`requireUser()` 守卫、**数字字段校验**：空→null / 非数字→报错 / 负数→拒绝，错误不写脏数据）；商家详情页加「增长前基准数据」区块。
  - **边界守住**：**非完整 MVS / Metric / KPI / ROI / 归因 / 图表**；`dataConfidence` 仅最小可信度标记（**未结构化 Evidence E1/E2/E3**）；权限仍简化（`baseline-actions.ts` 注释 TODO 待权限模型）。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（5 migrations, up to date）✓ / 本地手工 11 项全绿（含 item 11：非法输入 `abc`/`-5` 被拒并报错、`count` 仍 1、DB 未污染）。tag `checkpoint-p2-003-merchant-baseline`。
- **P2-004 TB-001 Minimal Intake Foundation 完成 ✅**（2026-06-03，TASK-030）：TB-001 **最小人工诊断 `MerchantDiagnosis`**（migration `20260603135056_p2_tb001_minimal_intake_foundation`；**1-1** with Merchant，`@unique`+`onDelete:Cascade`；`DiagnosisStatus{draft,completed,archived}`；摘要字段 + `sourceProfileId`/`sourceBaselineMetricId` **软引用**当前画像/基准；createdBy/updatedBy → `UserProfile.id`）。
  - 页面：`/dashboard/merchants/[id]/diagnosis`（创建/编辑，`saveMerchantDiagnosis` upsert，merchantId bind、`requireUser()` 守卫；**显示上游输入只读上下文**=画像+基准；保存时记录当前画像/基准 id 作上游引用）；详情页加「TB-001 商家诊断（最小）」区块（状态/摘要/引用画像·基准/更新人）；占位列表移除「TB-001 商家诊断」。
  - **边界守住**：**非完整 TB-001 表单 / 非 AI 诊断 / 非评分 / 非审核流 / 非自动策略 / 非 Evidence 复杂对象 / 非 MVS / 非 Experience**；权限仍简化（`diagnosis-actions.ts` 注释 TODO 待权限模型）。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（6 migrations, up to date）✓ / 本地手工：上游上下文显示画像+基准→创建(completed)→详情显示+引用画像/基准→更新(→archived)→DB `count=1`、`sourceProfileId/sourceBaselineMetricId` 匹配当前画像/基准、createdBy=updatedBy=当前 profile、updated_after_create→未登录 307。tag `checkpoint-p2-004-tb001-minimal`。
- **P2-005 TB-002 Account Setup Foundation 完成 ✅**（2026-06-03，TASK-031）：第二个模板节点**账号搭建资产 `MerchantAccountSetup`**（migration `20260603144206_p2_tb002_account_setup_foundation`；**1-1** with Merchant，`@unique`+`onDelete:Cascade`；`AccountSetupStatus{draft,completed,archived}`；平台/定位/命名/Bio/视觉/人设/GMaps/联系/风险 等摘要字段 + `sourceDiagnosisId` **软引用**当前 TB-001 诊断；createdBy/updatedBy → `UserProfile.id`）。
  - 页面：`/dashboard/merchants/[id]/account-setup`（创建/编辑，`saveMerchantAccountSetup` upsert，merchantId bind、`requireUser()` 守卫；**显示上游 TB-001 诊断只读上下文**；保存时记录当前 diagnosis id）；详情页加「TB-002 账号搭建（最小）」区块。
  - **边界守住**：**非完整 TB-002 Output / 非真实建号 / 非 TikTok·FB·IG·GMaps API / 非 AI / 非 Channel Asset / 非内容采集运营 / 非引流转化 / 非 MVS / 非审核流**；权限仍简化（`account-setup-actions.ts` 注释 TODO 待权限模型）。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（7 migrations, up to date）✓ / 本地手工 12 项全绿（上游显 TB-001 诊断→创建(completed)→详情显示+引用诊断→更新(→archived)→DB `count=1`、`sourceDiagnosisId` 匹配当前诊断、createdBy=updatedBy=当前 profile、updated_after_create→未登录 307）。tag `checkpoint-p2-005-tb002-account-setup`。
- **P2-006 TB-003 Material Collection Foundation 完成 ✅**（2026-06-03，TASK-032）：第三个模板节点**素材采集资产 `MerchantMaterialCollection`**（migration `20260603152402_p2_tb003_material_collection_foundation`；**1-1** with Merchant，`@unique`+`onDelete:Cascade`；`MaterialCollectionStatus{draft,completed,archived}`；素材分类/缺口/拍摄场景/人物/产品/信任/品牌故事/优先级/风险 等摘要字段 + `sourceAccountSetupId` **软引用**当前 TB-002 账号搭建；createdBy/updatedBy → `UserProfile.id`）。
  - 页面：`/dashboard/merchants/[id]/materials`（创建/编辑，`saveMerchantMaterialCollection` upsert，merchantId bind、`requireUser()` 守卫；**显示上游 TB-002 账号搭建只读上下文**；保存时记录当前 account setup id）；详情页加「TB-003 素材采集（最小）」区块。
  - **边界守住**：**非完整 TB-003 Output / 非文件上传 / 非素材库 / 非图片视频存储 / 非素材审核 / 非 AI 分析 / 非内容运营 / 非直播 / 非引流转化 / 非 MVS / 非第三方 API**；权限仍简化（`material-actions.ts` 注释 TODO 待权限模型）。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（8 migrations, up to date）✓ / 本地手工 12 项全绿（上游显 TB-002 账号搭建→创建(completed)→详情显示+引用账号搭建→更新(→archived)→DB `count=1`、`sourceAccountSetupId` 匹配当前账号搭建、createdBy=updatedBy=当前 profile、updated_after_create→未登录 307）。tag `checkpoint-p2-006-tb003-material`。
- **P2-007 TB-004 Content Operation Foundation 完成 ✅**（2026-06-03，TASK-033）：第四个模板节点**内容运营资产 `MerchantContentOperation`**（migration `20260603153629_p2_tb004_content_operation_foundation`；**1-1** with Merchant，`@unique`+`onDelete:Cascade`；`ContentOperationStatus{draft,completed,archived}`；内容定位/栏目/比例/发布频率/风格/禁区/前30天计划/风险 等摘要字段 + `sourceMaterialCollectionId` **软引用**当前 TB-003 素材采集；createdBy/updatedBy → `UserProfile.id`）。
  - 页面：`/dashboard/merchants/[id]/content-operation`（创建/编辑，`saveMerchantContentOperation` upsert，merchantId bind、`requireUser()` 守卫；**显示上游 TB-003 素材采集只读上下文**；保存时记录当前 material collection id）；详情页加「TB-004 内容运营（最小）」区块。
  - **边界守住**：**非完整 TB-004 Output / 非内容发布 / 非内容日历 / 非脚本·AI 文案 / 非图文视频生产 / 非素材库 / 非平台 API / 非直播 / 非引流转化 / 非 MVS**；权限仍简化（`content-operation-actions.ts` 注释 TODO 待权限模型）。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（9 migrations, up to date）✓ / 本地手工 12 项全绿（上游显 TB-003 素材采集→创建(completed)→详情显示+引用素材采集→更新(→archived)→DB `count=1`、`sourceMaterialCollectionId` 匹配当前素材采集、createdBy=updatedBy=当前 profile、updated_after_create→未登录 307）。tag `checkpoint-p2-007-tb004-content-operation`。
- **P2-008 TB-005 Live Planning Foundation 完成 ✅**（2026-06-03，TASK-034）：第五个模板节点**直播规划资产 `MerchantLivePlanning`**（migration `20260603154731_p2_tb005_live_planning_foundation`；**1-1** with Merchant，`@unique`+`onDelete:Cascade`；`LivePlanningStatus{draft,completed,archived}`；可行性/平台/目标/形式/主题/频率/人员/准备度/风险 等摘要字段 + `sourceContentOperationId` **软引用**当前 TB-004 内容运营；createdBy/updatedBy → `UserProfile.id`）。
  - 页面：`/dashboard/merchants/[id]/live-planning`（创建/编辑，`saveMerchantLivePlanning` upsert，merchantId bind、`requireUser()` 守卫；**显示上游 TB-004 内容运营只读上下文**；保存时记录当前 content operation id）；详情页加「TB-005 直播规划（最小）」区块。
  - **边界守住**：**非完整 TB-005 Output / 非直播排期系统 / 非直播间管理 / 非推流开播 / 非脚本·AI 话术 / 非数据抓取 / 非平台 API / 非引流转化 / 非 MVS**；权限仍简化（`live-planning-actions.ts` 注释 TODO 待权限模型）。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（10 migrations, up to date）✓ / 本地手工 12 项全绿（上游显 TB-004 内容运营→创建(completed)→详情显示+引用内容运营→更新(→archived)→DB `count=1`、`sourceContentOperationId` 匹配当前内容运营、createdBy=updatedBy=当前 profile、updated_after_create→未登录 307）。tag `checkpoint-p2-008-tb005-live-planning`。
- **P2-009 TB-006 Lead Conversion Foundation 完成 ✅**（2026-06-03，TASK-035）：第六个模板节点**引流转化资产 `MerchantLeadConversion`**（migration `20260603172648_p2_tb006_lead_conversion_foundation`；**1-1** with Merchant，`@unique`+`onDelete:Cascade`；`LeadConversionStatus{draft,completed,archived}`；引流/转化路径、私域、活动、GMaps、投流测试、P-001 准备度、30天动作、归因方式、转化风险 等摘要字段 + **`sourceContentOperationId` + `sourceLivePlanningId` 双软引用**当前 TB-004/TB-005；createdBy/updatedBy → `UserProfile.id`）。
  - 页面：`/dashboard/merchants/[id]/lead-conversion`（创建/编辑，`saveMerchantLeadConversion` upsert，merchantId bind、`requireUser()` 守卫；**显示双上游只读上下文**=TB-004 内容运营 + TB-005 直播规划；保存时记录两者当前 id）；详情页加「TB-006 引流转化（最小）」区块。
  - **边界守住**：**非完整 TB-006 Output / 非广告投放 / 非 Campaign 执行 / 非 ROI 计算 / 非归因算法 / 非 MVS / 非真实活动执行 / 非私域工具 / 非各平台 API**；权限仍简化（`lead-conversion-actions.ts` 注释 TODO 待权限模型）。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（11 migrations, up to date）✓ / 本地手工 14 项全绿（双上游显 TB-004+TB-005→创建(completed)→详情显示+双引用→更新(→archived)→DB `count=1`、`sourceContentOperationId/sourceLivePlanningId` 均匹配当前、createdBy=updatedBy=当前 profile、updated_after_create→未登录 307）。tag `checkpoint-p2-009-tb006-lead-conversion`。
- **P2-010 TB-007 Data Review Foundation 完成 ✅**（2026-06-03，TASK-036）：第七个模板节点**数据复盘资产 `MerchantDataReview`**（migration `20260603181741_p2_tb007_data_review_foundation`；**1-1** with Merchant，`@unique`+`onDelete:Cascade`；`DataReviewStatus{draft,completed,archived}`；复盘周期、目标完成度、内容/直播/引流转化效果、真实经营数据、问题诊断、优化建议、策略判断、归因观察、复盘风险 等摘要字段 + **`sourceBaselineMetricId`+`sourceContentOperationId`+`sourceLivePlanningId`+`sourceLeadConversionId` 四软引用**当前基准/TB-004/005/006；createdBy/updatedBy → `UserProfile.id`）。
  - 页面：`/dashboard/merchants/[id]/data-review`（创建/编辑，`saveMerchantDataReview` upsert，merchantId bind、`requireUser()` 守卫；**显示四上游只读上下文**=基准 + TB-004 + TB-005 + TB-006；保存时记录四者当前 id）；详情页加「TB-007 数据复盘（最小）」区块。
  - **边界守住**：**非完整 TB-007 Output / 非完整 MVS / 非自动归因 / 非 ROI 计算 / 非报表图表 / 非数据抓取 / 非平台 API / 非 Experience 自动入库 / 非 AI 复盘 / 非审核流**；RealMetric/AttributionRecord/ROI/KPI/ReviewFinding/ExperienceCandidate/Report 等表均未建。权限仍简化（`data-review-actions.ts` 注释 TODO 待权限模型）。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（12 migrations, up to date）✓ / 本地手工 18 项全绿（四上游显基准+TB-004/005/006→创建(completed)→详情显示+四引用→更新(→archived)→DB `count=1`、四 source id 均匹配当前、createdBy=updatedBy=当前 profile、updated_after_create→未登录 307）。tag `checkpoint-p2-010-tb007-data-review`。
- **P2-011 TB-008 90-Day Growth Plan Foundation 完成 ✅**（2026-06-03，TASK-037）：第八个模板节点**90天增长计划资产 `MerchantNinetyDayGrowthPlan`**（migration `20260603184126_p2_tb008_ninety_day_growth_plan_foundation`；**1-1** with Merchant，`@unique`+`onDelete:Cascade`；`NinetyDayGrowthPlanStatus{draft,completed,archived}`；计划周期、三阶段目标、路线图、平台优先级、内容/引流路线、KPI、风险、周期判断、下一阶段方向 等摘要字段 + **`sourceBaselineMetricId`+`sourceDiagnosisId`+`sourceLeadConversionId`+`sourceDataReviewId` 四软引用**当前基准/TB-001/006/007；createdBy/updatedBy → `UserProfile.id`）。
  - 页面：`/dashboard/merchants/[id]/growth-plan`（创建/编辑，`saveMerchantNinetyDayGrowthPlan` upsert，merchantId bind、`requireUser()` 守卫；**显示四上游只读上下文**=基准 + TB-001 + TB-006 + TB-007；保存时记录四者当前 id）；详情页加「TB-008 90天增长计划（最小）」区块。
  - **边界守住**：**非完整 TB-008 Output / 非完整 90天执行系统 / 非自动排期 / 非 KPI 自动计算 / 非 AI 生成 / 非 MVS / 非 Experience / 非计划任务系统 / 非报表 / 非平台 API**；权限仍简化（`growth-plan-actions.ts` 注释 TODO 待权限模型）。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（13 migrations, up to date）✓ / 本地手工 18 项全绿（四上游显基准+TB-001/006/007→创建(completed)→详情显示+四引用→更新(→archived)→DB `count=1`、四 source id 均匹配当前、createdBy=updatedBy=当前 profile、updated_after_create→未登录 307）。tag `checkpoint-p2-011-tb008-growth-plan`。
- **🎯 里程碑：TB-001~008 模板节点最小地基全链完成**（P2-004~011）。商家详情页自上而下：画像 → 基准 → TB-001 诊断 → TB-002 账号搭建 → TB-003 素材采集 → TB-004 内容运营 → TB-005 直播规划 → TB-006 引流转化 → TB-007 数据复盘 → TB-008 90天增长计划，逐级软引用上游，形成"诊断→执行→复盘→再规划"最小回路；全部人工录入（守 AI 不拍板）。
- **P2 审计 + 轻量重构完成 ✅**：
  - **TASK-038 P2 链路只读审计**（`docs/project/p2-chain-review-and-refactor-check-v1.md`，tag `checkpoint-p2-chain-review`）：主链健康；建议先 **A 轻量重构 → B 权限模型**；最大债=零鉴权。
  - **TASK-039 P2 轻量重构**（详情页展示层，tag `checkpoint-p2-refactor-presentation`）：抽出 `app/components/merchants/`（`MerchantAssetSection` / `AssetSummaryGrid` / `StatusBadge` / `format`{`formatDateTime`,`referenceLabel`}）；详情页 **620 → 446 行（-28%）**、重复 section/dl/空态 shell 集中到 4 个可复用组件；状态统一为徽章；**行为/展示逐字段保持**（唯一可见变化：状态由纯文本 → 统一徽章）；**无 schema/migration/DB/URL/action/节点页变更**；build/lint/migrate-status 全绿 + 手工回归 9 项通过。Action helper（审计 #5）按风险控制**留到下一轮**。
- **P2-012 Permission Foundation 完成 ✅**（2026-06-04，TASK-040）：把"登录即可访问全部商家"升级为**最小商家级权限隔离**——`admin` 全见全写；其它角色（operator 等）仅 `ownerProfileId|createdByProfileId` 命中的商家可见可写。**未改 schema**（复用 Merchant 既有 owner/createdBy 字段）、**无新 migration**、**仅测试数据**（已删，DB 还原）。
  - 统一 helper `app/lib/merchants/permissions.ts`：`canAccessMerchant`（谓词）/ `merchantVisibilityWhere`（list 的 Prisma where 片段）/ `assertMerchantWriteAccess`（action 写守卫，返回错误串或 null）。
  - data 层强制：`listMerchants(user)` 套可见性 where；`getMerchantById(id, user)` 改 `findFirst({id, ...可见性})`→无权返回 null（页面 `notFound()` 404，不泄露存在性）。**11 个商家页面**（详情 + 10 节点）+ list 页均传 user。
  - **10 个节点 action** 在 `requireUser()` 后加 `assertMerchantWriteAccess` 守卫（无权返回「无权操作该商家。」、不写库）；`createMerchant` 无需（建者即 owner）。各 action 旧"TODO 鉴权"由真实守卫取代；**baseline 数字校验保持不变**。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（13 migrations·无变更）✓ / **回归两层全绿**：Tier1（对真实数据跑 helper 同款查询/谓词：operator 仅见商家A、admin 见全部；`getMerchantById(他人商家)` operator→null·admin→found；写谓词 operator 拒·admin 放；operator 对自有商家放行）；Tier2（**真实 HTTP 全链路**：service_role 铸会话→ operator GET 列表仅含商家A、GET 他人商家详情=**404**、GET 自有=200；切 admin→列表含全部、他人商家=200；角色还原 operator）。tag `checkpoint-p2-012-permission-foundation`。
  - **边界守住**：**非复杂权限矩阵 / 非 org·tenant / 非商家门户 / 非邀请 / 非 billing / 非完整 RBAC UI**；其它非 admin 角色暂统一为"仅自有"（最小安全默认，按角色细分留后续）。
- **P2-013 Merchant Workspace / Node Overview 完成 ✅**（2026-06-04，TASK-041）：为每个 Merchant 提供**内部运营导航工作台** `/dashboard/merchants/[id]/workspace`——一页看全资产链路状态 + 快速入口。**不新增业务模型 / 不改 schema / 无 migration / 不改业务行为 / 非 Workflow·AI·审核·MVS**。
  - 新增只读 helper `app/lib/merchants/workspace.ts`：`buildMerchantWorkspace(merchant)`（纯数据整理，**不查 DB**，吃 `getMerchantById` 已 include 的 merchant）/ `getNodeStatus(asset)`（missing/draft/completed/archived；Profile·Baseline 无 status 字段→present 即 completed）/ `getFirstMissingNode(nodes)`；下一步为**规则提示**（链路首个 missing），文案守"建议优先补齐 / 可考虑下一步 / 不代表业务决策"，**非 AI 决策**。
  - 新增页面 `/dashboard/merchants/[id]/workspace`（`requireUser()` + `getMerchantById(id, user)`，**复用 TASK-040 权限**，无权 404）：基础信息（名称/行业/城市·国家/状态/owner/createdBy）+ 下一步规则提示卡 + Profile→Baseline→TB-001~008 全 10 节点（名称/状态徽章/简述/更新时间/是否引用上游/进入·创建入口）。新增展示组件 `app/components/merchants/workspace-node-row.tsx`；`StatusBadge` 加 `missing` 样式（加性，不改现有渲染）。
  - 入口：详情页头部加「打开工作台」、列表页每行加「工作台」链接（不删原区块）。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（13 migrations·无变更）✓ / **真实 HTTP 手工 12 项全绿**（登录→列表含工作台入口→商家A 工作台显示基础信息+全 10 节点+下一步+免责声明→空商家 10×missing 且下一步=优先补齐 Profile→节点入口 /profile·/baseline 可达→详情页含打开工作台→未登录 307→operator 访他人商家工作台 404）；测试商家已清理，acct 角色仍 operator。tag `checkpoint-p2-013-merchant-workspace`。
- **P2-014 Smoke Test & 测试数据卫生地基 完成 ✅**（2026-06-04，TASK-042）：建立**最小、可重复、低风险**的 P2 主链 smoke test + 测试数据自动清理。**不改业务逻辑/权限/schema、无 migration、无业务模型**。
  - 新增 `app/scripts/smoke-p2.ts`（DB + helper 层；**不碰 session/cookie/token → 零密钥泄露面**）：① env 必填存在性（仅打印**键名**）② 权限 helper（`canAccessMerchant` owner/other/admin + `merchantVisibilityWhere` 对真实 DB 查询的可见/拒绝/列表）③ workspace helper（满链商家→10 节点全非 missing·firstMissing=null·"最小链路已完整"·diagnosis 上游引用=true；空商家→10×missing·firstMissing=profile·"建议优先补齐"）④ 输出 PASS/FAIL 摘要 + 非零退出码。
  - 测试数据卫生：所有造数据带前缀 `SMOKE_TEST_`，`finally` 严格按 `name startsWith SMOKE_TEST_`（+临时 profile email 前缀）清理（失败也清）；Merchant 删除经 `onDelete:Cascade` 连带 11 资产；**绝不碰真实数据**。
  - 工具链：新增 devDep **`tsx`**（最小 TS runner，解析 tsconfig `@/` paths，无大型/浏览器依赖）；npm script **`smoke:p2`**（`node --env-file=.env --import tsx`）；`tsconfig.exclude` 加 `scripts`（脚本由 tsx 跑、不进 next build 类型检查）。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（13 migrations·无变更）✓ / **`npm run smoke:p2` = 17/17 PASS·cleanup CLEAN·exit 0**；跑后 DB 无 `SMOKE_TEST_` 残留；输出无密钥；每步具名→失败可定位。tag `checkpoint-p2-014-smoke-test`。（注：`npm i` 报 2 个 esbuild 相关 moderate 漏洞，属 tsx 的 dev 依赖、仅开发期，未 `audit fix --force`。）
- **架构（业务分析·无代码）：商家经营健康检查体系 V1 完成 ✅**（2026-06-04，TASK-043）：新增 [`docs/project/merchant-operating-health-check-architecture-v1.md`](./merchant-operating-health-check-architecture-v1.md)——吸收"小吃车五脏俱全"模型，建立**五器官经营健康判断框架**（渠道/Offer/履约/现金流/组织），增长前横向体检"商家是否接得住放大"。**纯文档，未改代码/schema/migration**。**关键发现**：现有 TB 链路强在"做流量"，但**履约(Fulfillment) + 组织(Organization) 两格几乎无专门资产节点**=最易"增长变风险放大器"的漏血点，建议后续诊断/工作台先把这两格显性化。守 Human Commercial Authority（只评估、不决定是否合作）。tag `checkpoint-operating-health-check-v1`。
- **P2-015 Operating Health Workspace Snapshot 完成 ✅**（2026-06-04，TASK-044）：把 OHC 五器官框架（TASK-043）落成**工作台只读观察层**——`/dashboard/merchants/[id]/workspace` 新增「经营健康摘要（五器官）」区块。**纯只读规则映射，不新增模型/migration/schema、不写经营健康数据、无评分/AI/诊断/决策、不改权限/Server Actions/URL、无新依赖。**
  - 新增只读 helper `app/lib/merchants/operating-health.ts`：`buildOperatingHealthSnapshot(merchant)`（纯函数·不查 DB·吃已 include 的 merchant）→ `{ organs[5], firstAttentionOrgan, missingEvidenceCount, hasCriticalAttention }`；状态 `signal/attention/missing/unknown`，每器官含 观察/数据来源/数据缺口/非决策下一步（"建议补充/关注/可在诊断中确认"）。**Channel/Offer/Cashflow 用现有字段；Fulfillment/Organization 无专门模型→标 `weakSignalOnly` 且文案"当前仅为弱信号，后续需要专门采集"，不伪装健康。`hasCriticalAttention` 仅作展示风险提示、不作决策。**
  - 新增展示组件 `app/components/merchants/operating-health-summary.tsx`；`StatusBadge` 加 `signal/attention/unknown` 样式（加性）；workspace 页插入区块（基础信息→下一步→**五器官摘要**→节点链路）。
  - smoke 扩展：`npm run smoke:p2` 加 8 项五器官校验（满链 5 器官无 missing；空商家 5 器官有 missing/unknown 且不伪装 signal；履约/组织空商家=unknown+weakSignalOnly）。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（13·无变更）✓ / **`smoke:p2` 25/25 PASS·cleanup CLEAN**；**真实 HTTP 手工**：商家A 工作台含五器官摘要(标题/免责/五器官/来源/下一步)、空商家显 missing+unknown+弱信号标注（不伪装健康）、未登录 307、operator 访他人工作台 404。tag `checkpoint-p2-015-operating-health-snapshot`。
- **P2-016 Fulfillment & Organization Intake 完成 ✅**（2026-06-04，TASK-045）：补上 OHC 暴露的**两个经营空格**——新增最小经营承接能力采集资产 `MerchantOperatingCapacity`，把履约/组织从"弱信号"变成可采集、可追溯的经营事实。**轻量功能：新增模型 + migration + 页面 + Server Action；无 AI/评分/MVS/审核流/完整 OHC；不改权限规则、不破坏已有表。**
  - 模型：`MerchantOperatingCapacity`（migration `20260604012735_p2_operating_capacity_foundation`；**1-1** with Merchant，`@unique`+`onDelete:Cascade`；`OperatingCapacityStatus{draft,completed,archived}`；履约 7 字段[响应流程/时效/预约/产能/高峰/履约风险/客户体验风险] + 组织 6 字段[老板依赖/人员分工/可委派/标准流程/培训准备/组织风险] + 综合[经营约束/备注]；createdBy/updatedBy → `UserProfile.id`）。**只摘要、不建 Staff/Schedule/SOP/Task/客服/财务表。**
  - 页面 `/dashboard/merchants/[id]/operating-capacity`（`requireUser()` + `getMerchantById(id,user)` 复用 TASK-040 权限·无权 404；免责"不代表系统自动决策"；履约/组织/综合分组表单，全可选，status 默认 draft，保存回工作台）；Server Action `saveMerchantOperatingCapacity`（`requireUser` + `assertMerchantWriteAccess` + upsert + redirect 工作台）；workspace 五器官区块加「补充履约与组织信息 / 编辑经营承接能力」入口。
  - **operating-health 升级**：Fulfillment/Organization 优先读 `MerchantOperatingCapacity`——有专项数据→状态升 `signal`/`attention`（risk 字段→attention）、来源加「经营承接能力采集」、缺口清空、`weakSignalOnly=false`；**无则回退弱信号、仍不伪装健康**。仍只读、非评分、非决策。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（14·up to date）✓ / **`smoke:p2` 30/30 PASS·cleanup CLEAN**（+5 项：满链建 capacity→履约 signal·组织风险 attention·来源含采集·空商家仍 unknown+弱信号）；**真实 HTTP 手工**：工作台入口、采集页 200+免责+履约/组织字段、录入后五器官读到「经营承接能力采集」且组织风险显 attention、未登录 307、operator 访他人采集页 404。tag `checkpoint-p2-016-operating-capacity`。
- **文档（SOP·无代码）：首批真实商家试点接入操作手册 V1 完成 ✅**（2026-06-04，TASK-046）：新增 [`docs/project/pilot-merchant-intake-playbook-v1.md`](./pilot-merchant-intake-playbook-v1.md)——面向内部人员的 16 章接入 SOP（接入前确认→创建 Merchant→Profile→Baseline→Operating Capacity→TB-001→TB-002~006→适时 TB-007/008→工作台/五器官用法→就绪清单→何时不应引流→证据纪律→人类决策点）。**纯文档，未改代码/schema/migration。** 对齐证据宪法 E0–E3、五器官 OHC、MVS、Human Commercial Authority + 实际系统路由/字段。TOT 首份面向真实商家的内部接入手册。tag `checkpoint-pilot-intake-playbook-v1`。
- **门禁（只读评估·无代码）：首批真实商家试点就绪门禁 V1 完成 ✅**（2026-06-04，TASK-047）：新增 [`docs/project/pilot-readiness-gate-v1.md`](./pilot-readiness-gate-v1.md)——只读盘点 + 实跑 `smoke:p2`（**30/30 PASS·cleanup CLEAN·真实数据零改动**），12 维就绪评估，结论 **🟡 Conditional Go**（可启动 1 家·接入期·内部主导的真实试点，须满足：人工授权/范围最小/遵循 Playbook 证据纪律/基线+归因先行/承接达标再引流/MVS 后置/多人先分角色）；明确**不授权**放量·收费·MVS·多租户·上传·AI。**纯文档，未改代码/schema/migration/真实数据。** tag `checkpoint-pilot-readiness-gate-v1`。
- **P2-017 Pilot Intake UI Guidance 完成 ✅**（2026-06-04，TASK-048）：在商家接入相关页面加入**录入引导 + 证据纪律护栏**（纯 UI 文案）。**未改 schema/migration/模型/Server Action/权限/URL/字段/业务行为、无新依赖。**
  - 新增轻量只读组件 `app/components/merchants/intake-guidance-box.tsx`（props：`title` / `items[]` / `tone: info|warning|evidence`）。
  - 8 个页面顶部/表单前插入提示：新建商家（录入原则）、Profile（先事实后判断·Offer 3 秒可懂·不知道写待补充）、Baseline（无基线不能证明增长·低可信标 low/medium）、Operating Capacity（履约/组织决定能否承接增长·缺口未补不建议引流）、TB-001 诊断（非建议合集·须基于上游·缺证据写待验证）、TB-007 复盘（需真实结果·别强行 completed·结论仅经验候选）、TB-008 计划（无复盘依据只 draft·别造假计划）、Workspace（提示非系统决策·按 Playbook·当前 Conditional Go 未授权放量/投流/MVS）。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（14·无变更）✓ / **`smoke:p2` 30/30 PASS·cleanup CLEAN**（纯 UI 未影响数据层）；**真实 HTTP 手工**：8 页提示框均渲染、baseline 保存按钮 + operating-capacity 表单/免责仍在（保存功能不受影响）、未登录 workspace/new 均 307。tag `checkpoint-p2-017-pilot-intake-guidance`。
- **文档（线下采集工具·无代码）：首批真实商家访谈提纲与证据采集清单 V1 完成 ✅**（2026-06-04，TASK-049）：新增 [`docs/project/pilot-merchant-interview-evidence-checklist-v1.md`](./pilot-merchant-interview-evidence-checklist-v1.md)——17 章线下访谈提纲 + 证据清单，**逐字段映射**现有系统（Merchant/Profile/Baseline/OperatingCapacity/TB-001~006/Workspace/OHC）：各模块问题库 + 证据采集清单（必须/可选·模块·可信度）+ 红旗清单 + 系统映射总表 + 访谈后核对。核心铁律"宁可留空，不可编造"；对齐证据宪法 E0–E3、五器官、MVS、Human Commercial Authority。**纯文档，未改代码/schema/migration/数据库/真实数据。** tag `checkpoint-pilot-interview-checklist-v1`。
- **P2-018 Demo Merchant Seed 完成 ✅**（2026-06-04，TASK-050）：新增可重复生成 / 可清理 / 明确标记 **DEMO** 的演示商家种子，供内部演示 / 培训 / UX 验证。**轻量脚本，无 schema/migration/模型/业务/权限变更。**
  - 新增 `app/scripts/seed-demo-merchant.ts`（用 tsx，归属 `admin@tot.local` profile；profile 不存在则停止提示登录、**不创建 auth 用户/真实数据**）；npm script **`seed:demo`** / **`seed:demo:clean`**（`--clean`）。
  - DEMO 数据：商家名 `DEMO_小吃车增长样例`、每字段含演示文案、**每 notes 写明"演示数据，不得用于 MVS/经验库/ROI/归因/真实案例"**；围绕小吃车五器官（履约高峰压力 + 老板单点→工作台显 attention）；TB-007/008 标 draft「演示复盘/方向·非真实结果」；全 11 资产逐级软引用。
  - 清理严格 `name startsWith "DEMO_"`（资产 cascade），**不碰真实 / SMOKE_TEST_ 数据**；seed 幂等（先清后建，可恢复）。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（14·无变更）✓ / **`smoke:p2` 30/30 PASS（DEMO 存在时仍独立通过，cleanup 只动 SMOKE_TEST_）** / seed → 11/11 资产 / clean → 只删 DEMO（商家A 保留）/ 再 seed 恢复；**真实 HTTP 手工**：列表见 DEMO、工作台全 10 节点 + 五器官 signal/attention + 履约/组织读「经营承接能力采集」+ 链路完整 + DEMO 标记可见、节点入口可达。**末态保留 DEMO 数据**（供演示）。tag `checkpoint-p2-018-demo-seed`。
- **P2-019 Demo Data Safety Badge 完成 ✅**（2026-06-04，TASK-051）：给 DEMO 数据加 **UI 安全护栏**，防止内部人员误把演示数据当真实商家/案例。**纯 UI，无 schema/migration/模型/权限/业务行为变更。**
  - 新增 `app/components/merchants/demo-data-badge.tsx`：`isDemoMerchant(name)`（**仅按 `name startsWith "DEMO_"`，只用于展示、不用于权限/查询**）+ `DemoDataBadge`（compact 列表用小徽章「DEMO 数据」/ full 详情·工作台用完整提示「这是 DEMO 演示数据…不得用于 MVS/ROI/归因/经验库/真实案例」）。
  - 接入：列表页商家名旁 compact 徽章；详情页、工作台页顶部 full 提示（均**仅 DEMO 显示**，非 DEMO 不显）。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（14·无变更）✓ / **`smoke:p2` 30/30 PASS**（纯 UI 未影响）；**真实 HTTP 手工**：列表恰 1 枚可见 DEMO 徽章（第 2 次「DEMO 数据」为 RSC flight 回显）、DEMO 详情/工作台显完整提示、商家A 不显、五器官/链路/节点入口不受影响、未登录 307、权限不变。tag `checkpoint-p2-019-demo-badge`。
- **文档（现场工具包·无代码）：首批真实商家现场访谈与录入采集包 V1 完成 ✅**（2026-06-04，TASK-052）：新增 [`docs/project/pilot-intake-field-pack-v1.md`](./pilot-intake-field-pack-v1.md)——把 Playbook/访谈清单/证据纪律整理成**可打印/可勾选/可填写**的 15 节现场采集包（访谈前确认→基础信息→Profile/Baseline 快采→五器官采集→Operating Capacity→TB-001~006 最少问题→证据来源/红旗/不应引流勾选→系统录入顺序→访谈后复盘→撕下三句提醒）。每字段带 填写留白 + 证据来源栏 + ☐待补充 + 「→ 录入:对应系统页面」。**纯文档，未改代码/schema/数据库。** tag `checkpoint-pilot-field-pack-v1`。
- **演练（只读·无代码）：首批真实商家内部接入演练 V1 完成 ✅**（2026-06-04，TASK-053）：新增 [`docs/project/pilot-intake-dry-run-report-v1.md`](./pilot-intake-dry-run-report-v1.md)——用 DEMO 商家 + Field Pack 做端到端演练：**登录态实跑 DEMO 全部 13 页面（列表+工作台+11 录入页）= 13/13 全 200 且渲染预期**（护栏/预填/DEMO 徽章/五器官/链路完整）。12 节报告：Field Pack→系统映射 13/13 清楚、工作台可用、证据纪律到位、五器官可用（履约/组织经 OperatingCapacity 升为 attention 不再弱信号）；缺口均非阻塞（无上传/单 operator/页面无反向 Field Pack 标注/risk 高亮可选/真实数据未跑）；结论 **🟡 Conditional Go**（未发现阻塞真实接入的系统问题；真实接入仍是人工商业决策）。**纯文档，未改代码/真实数据**（DEMO 保持不变）。tag `checkpoint-pilot-dry-run-v1`。
- **架构（业务/产品/权限分析·无代码）：角色分权界面与环节交接体系 V1 完成 ✅**（2026-06-05，TASK-054）：新增 [`docs/project/role-based-ui-and-stage-handoff-architecture-v1.md`](./role-based-ui-and-stage-handoff-architecture-v1.md)——为 TOT 从"单人可用系统"升级为"多人协同运营系统"做设计。基于**真实 Role enum**（merchant/collector/operator/executor/admin/ai_worker）+ 现有权限（仅商家级 admin/own、无节点级）：8 类业务角色→6 枚举映射（标缺口）、账户可见性矩阵、节点操作矩阵、统一 UI 显示模型（顶部信息条+节点卡片+状态/证据状态对接 E0–E3）、环节交接模型（链+必含项+**人工确认非自动审批**）、审核/打回模型、人类决策边界、**分阶段路线图 A(UI 不动 DB)→B(角色视图+写保护)→C(MerchantStageHandoff 轻量模型)→D(审核/放行)**。**关键发现**：6 角色枚举粗粒度且未真正接入（默认 operator）、status 不足表达提交/审核/打回、无 Handoff/Review 记录——建议先 A 后 B、不提前引复杂 Workflow。**纯文档，未改代码/schema/权限逻辑。** tag `checkpoint-role-ui-handoff-arch-v1`。
- **P2-020 Role-Based Workspace UI · Phase A 完成 ✅**（2026-06-05，TASK-055）：落地角色分权架构（TASK-054）的 **Phase A——工作台 UI 统一显示层**（**只读展示**，不动 DB/权限/Server Action/交接模型/审核流/自动流转）。
  - 新增只读 helper `app/lib/merchants/workspace-role-ui.ts`：`getNodeRoleUI(key)`（每节点 UI meta：nodeCode/建议负责角色[**仅真实 enum** merchant/collector/operator/executor/admin/ai_worker + 业务解释]/上游输入/证据提示/交接提示/人工确认提示）+ `nextActionLabel(status)`（非决策文案）。
  - 新增组件 `workspace-stage-summary.tsx`（工作台顶部阶段摘要：当前负责人/链路状态/当前阶段/建议关注器官 + "系统提示不代表业务决策"）；增强 `workspace-node-row.tsx`（每节点卡片统一显示 编号徽章/建议负责/上游/证据/下一步/交接/人工确认，保留原有 label/状态/入口）。文案守"建议由…处理 / 需人工确认 / 不代表系统决策"，避免"已批准/自动通过/可放量"等。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（14·无变更）✓ / **`smoke:p2` 31/31 PASS**（+1：每工作台节点都有 role-ui meta）；**真实 HTTP 手工**：DEMO 工作台显阶段摘要 + 节点统一显示（角色/编号/上游/交接/人工确认）+ DEMO 徽章/五器官/节点入口仍在、非 DEMO 商家正常显示且无 DEMO 提示、未登录 307、权限不变。tag `checkpoint-p2-020-role-workspace-ui`。
  - ⚠ 过程注记：手工验证途中遇 **Turbopack dev 缓存损坏**（多次 build/dev/kill 残留多进程争写 `.next` → `TurbopackInternalError`/`.sst` 缺失 → 页面误报 404/500）；**非代码问题**（build 干净通过）。杀净所有 next 进程 + `rm -rf .next` + 单一 dev 后全部 200/绿。**经验：本机一次只跑一个 dev server，验证完务必杀净。**
- **P2-021 Role-Based UI Visibility & Write Guard · Phase B 完成 ✅**（2026-06-05，TASK-056）：在现有 6 角色枚举上做**最小节点级写保护 + 角色视图**（**不改 schema/migration/Role enum、不做 Handoff/Review/审核流/提交·打回·锁定状态、不改 URL/字段/业务数据**）。
  - 新增 helper `app/lib/merchants/role-access.ts`：`MerchantNodeKey`(12)/`canEditMerchantNode(role,node)`/`canViewMerchantNode`/`getRoleNodeAccess`(canEdit/canView/非决策原因)/`assertMerchantNodeWriteAccess(user,merchantId,node)`(**先商家级[TASK-040]再节点级**)/`canCreateMerchant`+`assertMerchantCreateAccess`/`nodeKeyForWorkspace`/`roleLabel`。映射：**admin 全部；collector=画像/基线/承接/素材；operator=诊断/复盘/计划/承接+协调；executor=账号/素材/内容/直播/引流；ai_worker & merchant 不可写**。
  - Server Action 写保护：**11 个节点 action** 由 `assertMerchantWriteAccess` 升级为 `assertMerchantNodeWriteAccess(…, "<nodeKey>")`；`createMerchant` 加 `assertMerchantCreateAccess`（admin/collector/operator 可建）。**保留商家级权限**（无权访问商家时即使 role 允许也拒）。
  - Workspace UI：顶部阶段摘要加「当前登录角色」；每节点卡片显示「可编辑/只读（建议）」徽章 + 原因（"建议由 X 编辑，如需修改请交给对应负责角色"）+ 不可编辑时按钮文案变「查看详情」。文案守非决策语义。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（14·无变更）✓ / **`smoke:p2` 43/43 PASS**（+12 角色校验：各角色 canEdit 矩阵 + ai_worker/merchant 全拒 + 商家级优先 + create-access）；**真实 HTTP 手工（角色驱动）**：operator 见可编辑+只读+查看详情+原因；切 admin→全可编辑无只读；切 collector→混合；非 DEMO 正常；未登录 307；角色还原 operator。tag `checkpoint-p2-021-role-write-guard`。⚠ 注：手工验证前已按 TASK-055 教训杀净所有 next 进程 + `rm -rf .next` + 单一 dev，避免 Turbopack 缓存损坏误报。
- **P2-022 Merchant Stage Handoff · Phase C 完成 ✅**（2026-06-05，TASK-057）：建立**最小人工"节点交接记录"**——记录 fromNode→toNode 交接,**只记录、不自动审批/流转/锁定/改节点状态**。
  - 模型：`MerchantStageHandoff`（migration `20260604211334_p2_stage_handoff_foundation`；Merchant **1-to-many**、`onDelete:Cascade`；`fromNode/toNode: MerchantStageNode`(13)、`status: MerchantStageHandoffStatus{submitted,received,cancelled}`、summary/gapSummary/riskSummary/evidenceSummary、submittedByProfileId + receivedByRole(Role) + receivedByProfileId? + reviewedByProfileId?(Phase D 占位) + receivedAt?/reviewedAt?）。**未做 approved/rejected/needs_revision/locked（属 Phase D）。**
  - Server Action `app/lib/merchants/handoff-actions.ts`：`createMerchantStageHandoff`（requireUser+商家访问+fromNode 提交权限[ai_worker/merchant 拒]→status=submitted→回工作台）/ `markMerchantStageHandoffReceived`（目标角色或 admin、submitted→received+receivedBy/At）/ `cancelMerchantStageHandoff`（提交人或 admin、submitted→cancelled）。权限 helper 加 `canSubmitHandoffFrom/canReceiveHandoff/canCancelHandoff`（role-access.ts）。
  - UI：工作台加「环节交接记录」区块（列表 + 「+ 新增交接记录」+ 角色感知 标记已接收/取消 按钮）；新增表单页 `/dashboard/merchants/[id]/handoffs/new`；`handoff-button.tsx`(client)/`handoff-section.tsx`；StatusBadge 加 submitted/received/cancelled 样式；getMerchantById include `stageHandoffs`。所有文案守"不代表自动审批,需人工确认"。
  - 验证：build ✓ / lint ✓ / `prisma migrate status`（15·up to date）✓ / **`smoke:p2` 49/49 PASS**（+6 交接：create/submit/receive/cancel 权限 + DB transition + 级联清理 handoffs=0）；**真实 HTTP 手工**：工作台交接区块+新增入口+免责、表单页 200、交接行渲染（diagnosis→account_setup·submitted·提交人）、**角色感知按钮**（operator/提交人→仅取消;executor/目标角色→标记已接收）、未登录 307、DEMO+五器官+角色 UI 仍在;DEMO 末态 0 交接。tag `checkpoint-p2-022-stage-handoff`。⚠ 手工验证前已杀净 next + `rm -rf .next` + 单一 dev（避 Turbopack 缓存损坏）。
- **文档（内部 SOP·无代码）：角色分工与环节交接操作指南 V1 完成 ✅**（2026-06-05，TASK-058）：新增 [`docs/project/role-and-handoff-operating-guide-v1.md`](./role-and-handoff-operating-guide-v1.md)——15 章内部操作指南,以**真实 Phase A/B/C** 为准（不写未实现的 Phase D）：6 角色定位/可编辑·只读·禁止 + 角色速查表 + 节点职责速查表 + 工作台怎么看 + 何时创建交接 + 交接表单怎么写 + 如何接收(received≠审批) + 如何取消(cancel≠打回) + 交接不能替代什么(决策/MVS/ROI/放量/合同) + DEMO 协同演练 + 常见错误 + 当前系统边界(已实现 vs 未实现) + 何时进 Phase D。**纯文档,未改代码/schema/权限。** tag `checkpoint-role-handoff-guide-v1`。
- **文档（内部 SOP·无代码）：内部角色账号准备与权限验证指南 V1 完成 ✅**（2026-06-05，TASK-059）：新增 [`docs/project/internal-role-account-setup-guide-v1.md`](./internal-role-account-setup-guide-v1.md)——15 章内部账号准备指南：当前 Auth/Role 状态（JIT 默认 operator、`admin@tot.local`=operator、角色需人工配置、无账号管理 UI）+ 账号类型 + 建议命名（**无密码**）+ 密码/密钥规则 + 人工创建流程 + **逐角色权限验证清单**（对照 role-access）+ DEMO/培训/真实员工/admin/角色变更/停用政策 + 试点前最少账号 + 未实现项 + 最终建议（**保留 admin@tot.local 为 operator,真实 admin 另建**）。**纯文档,未创建账号/未改角色/未改代码/未输出密码。** tag `checkpoint-internal-account-guide-v1`。
- **🚩 里程碑：P2 真实试点前系统封板完成 ✅**（2026-06-05，TASK-060）：新增 [`docs/project/p2-pre-pilot-system-freeze-handoff-summary-v1.md`](./p2-pre-pilot-system-freeze-handoff-summary-v1.md)——16 章封板交接快照,封板基准 HEAD `f51f715`、实测 **`smoke:p2` 49/49 PASS·git 干净·15 migrations·DEMO 在·真实试点商家 0·admin@tot.local=operator**。明确：已完成能力清单 / 模型·角色·权限·交接·DEMO·测试现状 / 当前可做与禁做 / 试点前检查清单 / 首个真实商家触发条件与第一天流程 / 已知延后项(Phase D·MVS·Experience·上传·账号 UI·多租户·门户·CI) / 停止条件 / 最终建议。**系统状态 = Pre-Pilot Ready / Conditional Go;不建议继续新增功能、不进 MVS/Phase D,建议等待真实商家。** 纯文档,未改代码/schema/账号/角色/真实数据。tag `checkpoint-p2-pre-pilot-freeze-v1`。
- **文档（商务 SOP·无代码）：首家试点商家商务沟通与合作邀约包 V1 完成 ✅**（2026-06-05，TASK-061）：新增 [`docs/project/first-pilot-merchant-outreach-kit-v1.md`](./first-pilot-merchant-outreach-kit-v1.md)——17 章内部商务工具包(帮线下人员找并沟通首家试点商家)：如何对商家讲 TOT(去行话) · 承诺边界(能说/不能说,**绝不承诺增长/ROI**) · 优先接触画像 · 首次开场话术 · 商家收益 · 商家需提供什么 · 启动前确认 · 何时不引流 · 试点范围(含/不含) · 首次面谈流程(用 Field Pack) · 面谈后内部判断(**人决定**) · 6 类话术模板 · 商务红线 · 接触阶段证据纪律 · 转入系统录入交接。**纯文档,未改代码/未建商家/未承诺增长。** tag `checkpoint-first-pilot-outreach-kit-v1`。
- **文档（商务 SOP·无代码）：首批试点商家候选名单与接触跟进表 V1 完成 ✅**（2026-06-05，TASK-062）：新增 [`docs/project/first-pilot-merchant-prospect-tracker-v1.md`](./first-pilot-merchant-prospect-tracker-v1.md)——12 章线下接触跟进工具：**可复制候选跟进表**(名称/行业/城市/联系人/来源/负责商务/当前状态/兴趣程度/愿资料采集/愿提供数据/愿复盘/适合 Field Pack/红旗/下一步/跟进时间/需 PM 判断) + 状态选项 + 兴趣程度参考 + Field Pack 就绪信号 + 红旗记录 + 下一步动作 + 跟进话术 + 不记敏感数据 + 何时提交 PM + 转入 Field Pack。**非系统筛选/非 AI 决策/非拒绝规则——人决定;不承诺增长。纯文档,未改代码/未建商家。** tag `checkpoint-first-pilot-prospect-tracker-v1`。
- **P3-001 Product Usability Shell & Dashboard Home 完成 ✅**（2026-06-05，TASK-063）：把系统从"底层链路能跑"推进到"**登录后可理解、可导航、可开始操作**"。**仅 UI/产品入口层：零新增依赖（现有 Tailwind 足够）、不改 schema/migration/权限核心/role-access、不接真实 AI API、不建账号/商家。**
  - 新增 `app/app/dashboard/layout.tsx` + `components/dashboard/dashboard-nav.tsx`：**所有 /dashboard 页面统一顶部导航**（TOT · 首页/商家管理/交接中心/AI 工作台 · 用户 email + roleLabel + 登出）——消灭孤岛页面。
  - 重写 `/dashboard` 首页：当前账号卡（email/角色+业务解释/状态）+ **系统状态卡（真实试点前准备态 Pre-Pilot Ready / Conditional Go,只有 DEMO、无真实商家、不投流不放量不承诺）** + 6 个快捷入口（新建商家/商家列表/**打开 DEMO（动态查 DEMO_,无则提示 seed:demo,不自动建）**/交接中心/AI 工作台/新手指南锚点）+ `onboarding-guide.tsx` 新手两路径（演示 5 步 / 真实商家 7 步,含 TB-001~008 中文对照）。
  - 新增 `/dashboard/handoffs` **交接中心 V0**（`lib/dashboard/home.ts` 的 `listRecentHandoffs` 复用商家可见性过滤;只读最近 20 条 + 链到对应工作台处理;空状态;免责"不代表自动审批"）;新增 `/dashboard/ai-workbench` **AI 工作台 V0 占位**（不接 API;AI 边界必读=不自动决策/不直接写入/必须人工审核;6 类草稿用途;当前人工辅助 4 步流程;下一阶段规划 5 步）。
  - 验证：lint ✓ / build ✓（新路由 /dashboard/handoffs、/dashboard/ai-workbench）/ **smoke 49/49** / **真实 HTTP 手工全绿**（首页 email/角色/状态/6 入口/DEMO 链工作台/新手两栏/TB 对照;导航出现在首页+列表+工作台;交接中心 200+空状态+免责;AI 工作台 200+V0+边界+规划;未登录 307）。tag `checkpoint-p3-product-usability-shell`。
- **P3-002 Merchant Intake Wizard 完成 ✅**（2026-06-05，TASK-064）：把分散的商家录入页面整合成**商家接入录入向导** `/dashboard/merchants/intake`。**仅 UI/录入路径整合：零新增依赖、不改 schema/migration/权限/role-access、复用现有页面·action·数据·权限。**
  - 向导页：标题 + 真实试点前准备态提醒 + 「录入前必读」护栏（先采集后录入/待补充/不编/不承诺/DEMO≠案例/AI 仅草稿）+ **6 步推荐顺序**（创建商家→画像→基线→履约组织→TB-001 诊断→工作台查缺口），每步统一结构（编号/名称/**这一步填什么/为什么重要/不知道怎么填**/入口按钮/提醒），新组件 `intake-step-card.tsx`；附 TB-002~008 中文对照与"复盘/计划无真实结果不强行完成"提醒。
  - **DEMO 学习区**（有 DEMO_→链其工作台+「不得当真实案例」；无→提示 seed:demo，不自动建）+ **最近商家继续录入**（`listMerchants(user)` 权限过滤取 5 条：名称/DEMO 徽章/创建时间/详情/工作台；空态指引）。
  - 入口：首页快捷卡片「商家接入向导」（置首位）+ 新手引导真实路径改链向导；商家**详情页与工作台头部**加「接入向导」返回入口（轻量,未大改）。路由 `/merchants/intake` 静态段优先于 `[id]`,无冲突（实测 [id] 仍 200）。
  - 验证：lint ✓ / build ✓（新路由）/ **smoke 49/49** / **真实 HTTP 手工全绿**（向导页全要素 6 步+必读+DEMO 区+最近商家+TB 对照；首页卡片+引导链接；详情/工作台返回入口；[id] 不受影响；未登录 307）。tag `checkpoint-p3-merchant-intake-wizard`。
- **P3-003 AI Workbench Prompt Flow 完成 ✅**（2026-06-05，TASK-065）：AI 工作台从占位页升级为**可实际使用的人工辅助版 AI 草稿工作流 V0**——选商家→选任务→系统整理上下文→生成可复制 Prompt→人工到 AI 工具生成→粘贴回来人工审核→**方案 A 跳转对应节点页人工保存**。**不接任何 AI API、AI 输出绝不自动保存、保存仍受 role-access 限制；零新增依赖、不改 schema/migration/权限。**
  - lib：`ai-workbench/tasks.ts`（**7 类任务** A-G：诊断/账号搭建/素材采集/内容运营/线索转化/数据复盘[带"无真实数据勿强行复盘"警告]/90天计划,各带目标节点+输出结构）；`context.ts`（只读整理已选商家→纯文本上下文：基础信息/画像/基线/履约组织/**链路状态+五器官（复用 workspace+OHC helper）**/明确缺失清单,**空字段一律标「待补充」不美化**）；`prompts.ts`（**8 条安全规则**：仅基于上下文/不编造/不承诺增长·ROI/区分事实·判断·风险·待补充·建议/中文/仅草稿需人工审核 + DEMO 加"不得当真实案例"+任务级警告）。
  - UI：重写 `/dashboard/ai-workbench`（URL 驱动 `?merchantId=&task=`,**Next16 searchParams await**;①商家选择[listMerchants 权限过滤前 8+DEMO 徽章+空态引导接入向导] ②7 任务卡片 ③上下文预览[完成度+缺失计数] ④Prompt 预览+`copy-prompt-button.tsx`(原生 clipboard) ⑤`manual-result-capture.tsx`(粘贴+审核提醒+复制审核稿+**去节点编辑页保存链接**)）；**无权/不存在 merchantId 统一提示不泄露**；工作台头部加「用 AI 生成草稿」(带 merchantId)；首页 AI 卡片文案更新。
  - 验证：lint ✓ / build ✓ / **smoke 54/54**（+5：7 任务/上下文成形/空商家标待补充/Prompt 含安全规则+结构）/ **真实 HTTP 手工全绿**（基础态/自动选中+DEMO 警告/Prompt+复制+回填+节点链接[2 项水合标记假阴性已分开匹配复核为真]/data_review 警告/无权不泄露/工作台入口/未登录 307）。tag `checkpoint-p3-ai-workbench-prompt-flow`。
- **P3-004 UI Polish & Launch Readiness 完成 ✅**（2026-06-11，TASK-066）：上线前产品级 UI 打磨——从"功能可用"进入"**产品可上线试用**"。**零新增依赖（Tailwind 足够）、不改 schema/migration/权限/role-access/业务逻辑。**
  - 新增 `components/ui/`：`button.tsx`（**编码按钮三层级** btnPrimary/btnSecondary/btnDanger+Sm 变体,固化既有约定）/ `page-header.tsx`（统一头部:标题+一句话说明+状态标签+操作区）/ `empty-state.tsx`（友好空态:标题+提示+行动按钮）。
  - 新增 **`/dashboard/launch-readiness` 上线前检查页**（实时:DEMO 存在/可见商家数[权限过滤];人工清单:smoke/Field Pack/Outreach Kit/候选表/账号角色/不承诺增长/AI 仅草稿;Conditional Go 启动条件提醒;**只提醒不放行**）;导航加「接入向导」「上线检查」。
  - 首页上线化：欢迎语+状态标签 / **今日建议动作**（规则提示:无商家→seed·向导;仅 DEMO→练习+线下接触;有真实→查缺口）/ 快捷入口**三分组**（商家接入与管理 / AI 辅助与交接协同 / DEMO 学习与上线准备）。
  - 页面统一：列表/详情/工作台/接入向导/AI 工作台/交接中心全部采用 PageHeader+按钮层级;**StatusBadge 加中文对照**（draft 草稿/completed 已完成/missing 未创建/submitted 已提交交接/active 合作中…,加性）;列表空态/交接空态/AI 无商家空态升级为 EmptyState+行动按钮;列表状态文本→徽章+表格 `overflow-x-auto`（小屏）;工作台头部主操作=**用 AI 生成草稿(主)+新增交接+详情/向导/列表**;AI 工作台步骤编号 ③上下文④Prompt⑤粘贴审核;4 个关键表单（画像/基线/承接/诊断）副标题加**保存后去向提示**。
  - 验证：lint ✓ / build ✓（新路由 launch-readiness）/ **smoke 54/54** / **真实 HTTP 手工 9/9 组全绿**（首页/导航/检查页/列表/交接空态/AI 编号/工作台/表单提示/未登录 307）。tag `checkpoint-p3-ui-launch-readiness`。
- **🚩 里程碑：生产环境 Release Candidate 验收完成 ✅**（2026-06-11，TASK-067）：新增 [`docs/project/production-release-candidate-verification-v1.md`](./production-release-candidate-verification-v1.md)——对 `tot-dun.vercel.app` 做只读最终验收：本地 lint/smoke(54/54)/build 全绿;**生产未登录+登录态 60+ 项检查零 FAIL**（公开路由+/health connected、6 内部页 307 拦截、首页/导航 8 页/接入向导 6 步/列表/DEMO 详情+工作台/AI 工作台 7 任务+Prompt+不泄露/交接中心/上线检查页、admin@tot.local 仍 operator、生产代码=最新 HEAD）。**结论 🟡 Conditional Pass**（唯一受限:多角色账号不足,跨角色线上复验待补充[本地已验];登出交互与小屏真机为观察项）;**0 Blocker/Major/Minor**。**建议可进入首家真实商家试点**（负责人授权,沿用 Conditional Go 条件）。未改任何代码/数据/角色,无密钥输出。tag `checkpoint-production-release-candidate-verification`。
- **P3-005 Virtual Pilot UAT Sandbox 完成 ✅**（2026-06-11，TASK-068）：建立**虚拟商家全流程测试沙盒**——真实客户进入前,系统可用 `UAT_` 数据完整压测。**不改 schema/migration/权限/role-access;UAT≠DEMO≠SMOKE≠真实商家,互不影响。**
  - 脚本：`scripts/seed-uat-merchants.ts`（npm **`seed:uat`**/**`seed:uat:clean`**,幂等,清理严格 `name startsWith "UAT_"` 级联资产）+ `scripts/uat-full-flow.ts`（npm **`uat:full-flow`**,只读全链测试）。
  - **3 个场景商家**：A `UAT_河内咖啡店完整链路`（资料完整,链路顺畅推进,missing=0）/ B `UAT_美甲店资料缺失场景`（**诚实处理"不知道"**:14 项待补充显式标注,不编造）/ C `UAT_小吃车承接能力不足场景`（履约+组织双 **attention**,TB-001 提示"不适合马上放量,先补承接"）。
  - **full-flow 31/31 PASS**：存在性/权限可见性/详情读取/工作台链路/五器官/AI 上下文/**3×7=21 个 Prompt 全含 UAT 警告+安全规则**/B 缺失诚实/C 风险识别(A 对照)/DEMO 未误删/SMOKE 无残留/真实商家不受影响/命名互斥。
  - UI：`isUatMerchant`+`UatDataBadge`（indigo,与 DEMO rose 区分;compact+full）接入 列表/详情/工作台/AI 工作台(行徽章+选中警告)/向导最近商家;AI 上下文加 `isUat` 标记、Prompt 加规则「这是 UAT 虚拟测试数据…不得当真实案例/不得对外引用」。
  - 验证：lint ✓/build ✓/**smoke 54/54**/**seed:uat ✓ → full-flow 31/31 ✓ → HTTP 手工 5 组全绿（UAT 徽章/警告/AI 识别/Prompt 警告/DEMO 不受影响）→ seed:uat:clean ✓（UAT=0·资产 0 残留·DEMO=1·SMOKE=0·真实完好）→ 清理后 full-flow 行为可解释**（明确提示先 seed,非零退出）。未发现需修复的 UI/流程 bug。末态:UAT 已清理（随时 `seed:uat` 重建）。tag `checkpoint-virtual-pilot-uat-sandbox`。
- **下一步（待用户定）**：**⑦ 用候选跟进表建名单 → 按 Outreach Kit 接触 → Field Pack 线下采集 → 接入向导录入 → AI 工作台辅助草稿(人工授权后,Conditional Go 条件下)** 最具价值；团队可先用 `seed:uat` 沙盒演练全流程；多人协同前可按账号指南建分角色账号并补线上多角色权限复验；多人协同前可（人工·业务决策）按账号指南**在 Supabase 创建 collector/operator/executor/admin 分角色账号 + 跑角色权限验证**；多人协同可继续角色 UI 路线图 **Phase D（审核/放行 + approved/rejected/needs_revision/locked 状态、打回机制）**,待真实试点后慎重；其余可选 ① 角色按需细分（collector/executor/… 差异化可见·可写，或第 7 角色 `outsource` 定夺）② 某 TB 深化 / ③ MVS·Metric·Experience（放量/收费前）/ ④ action-helper 轻量重构（审计 #5，第二轮）/ ⑤ 经营承接能力深化（履约/组织 risk 在工作台高亮、纳入诊断维度）/ ⑥ smoke 扩展（HTTP 层/CI）。注：`admin@tot.local` 现仍为 `operator` 角色（仅见自有商家）；若需全见，需人工将其置为 `admin`（角色=业务决策，AI 不拍板）。仍守 AI 不拍板 / 人工审核。

## 待决 / 待用户提供
- **P1 登录本地 + 线上均已验证 ✅**（TASK-025/026）。Vercel 已配 `NEXT_PUBLIC_SUPABASE_URL`+`NEXT_PUBLIC_SUPABASE_ANON_KEY`（service_role 未用、未配）。
  - service_role key（新版 `sb_secret_`）：P1 未用，配不配均可；若配**仅服务端、绝不入库/不暴露浏览器**。
  - 测试账号 `admin@tot.local`/`TotAdmin#2026` 为临时夹具，上线前改密码或删除。
- **角色差异（需你定夺）**：角色枚举按 `ROLE_MODEL.md` 用 **6 个**（merchant/collector/operator/executor/admin/ai_worker）。SYSTEM_BLUEPRINT 另有 **`outsource`（第7个）**——已知文档差异（project-audit R2）。P1 **未发明、未删减**；若要纳入 outsource，后续加一个 migration 即可。
- **P1 默认角色**：新用户首登默认 `operator`（占位；P1 不做角色强制，仅"登录与否"门禁）。生产前需做**角色分配 + 限制开放注册**。
- **安全**：之前暴露过的 GitHub PAT / Vercel Token `vck_…` 请尽快 revoke（若未做）。

## 接续要点（换窗口/换机/换 AI）
- 仓库 `github.com/otcweb3admin-art/TOT`（monorepo）。**Vercel Root Directory 必须 = `app`**。线上 `https://tot-dun.vercel.app`。
- 代码：`cd app && npm install && npm run dev`。密钥在本机 `app/.env`（gitignored）。
- ⚠️ **Next 16 把 middleware 改名为 `proxy`**（`app/proxy.ts`，Node 运行时）；`cookies()` 是 async。写码前读 `app/node_modules/next/dist/docs/`。
- Prisma 在 Windows 偶发 `EPERM`（引擎 DLL 锁）——与 schema 无关，client 实际已生成，可忽略（必要时 kill node 后重试 generate）。
- 完整接续本对话：同机 `/resume`；否则靠仓库 + CLAUDE.md + 本文件。

---
> 更新纪律：完成一个工作块后，更新"一句话状态/当前阶段/下一步/待决"，随 docs 自动推送上线。
